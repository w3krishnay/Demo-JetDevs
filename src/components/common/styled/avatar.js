import styled from "styled-components";

const Avatar = styled.img`
  width: calc(96vh / 10 - 30px);
  height: calc(96vh / 10 - 30px);
  border-radius: calc(96vh / 10 - 30px);
  margin: 0 20px 0 0;
  box-shadow: 2px 2px 2px #888888;
`;

export default Avatar;
